---
interface Props {
  cols?: 2 | 3 | 4 | 5;
  showcase?: boolean;
  variant?: string;
}
const { cols = 2, showcase = false, variant } = Astro.props;
---
<div class:list={['oshi-card-grid', showcase && 'oshi-card-grid--showcase']} data-cols={cols} data-variant={variant}>
  <slot />
</div>
